// Allocate disk space and invalidate file privilege metadata on success
fallocate(fd, mode = FALLOC_FL_KEEP_SIZE, offset, size);

// Deallocate space and invalidate file privilege metadata on success
fallocate(fd, mode = FALLOC_FL_PUNCH_HOLE | FALLOC_FL_KEEP_SIZE, offset, size);
